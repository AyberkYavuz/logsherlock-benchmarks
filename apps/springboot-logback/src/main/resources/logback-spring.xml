<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSS} %-5level [%thread] %logger{0} application=%X{application} environment=%X{environment} schemaVersion=%X{schemaVersion} scenario=%X{scenario} reqId=%X{reqId} traceId=%X{traceId} orderId=%X{orderId} customerId=%X{customerId} productId=%X{productId} paymentId=%X{paymentId} shipmentId=%X{shipmentId} service=%X{service} component=%X{component} %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
